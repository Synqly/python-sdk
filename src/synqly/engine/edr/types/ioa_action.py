# This file was auto-generated by Fern from our API Definition.

import typing

from ...core import enum

T_Result = typing.TypeVar("T_Result")


class IoaAction(enum.StrEnum):
    """
    The action the EDR takes when the IOA rule matches. `block` maps to the strongest enforcement the provider supports for the rule type (for example Block Execution or Kill Process).
    """

    MONITOR = "monitor"
    DETECT = "detect"
    BLOCK = "block"
    _UNKNOWN = "__IOAACTION_UNKNOWN__"
    """
    This member is used for forward compatibility. If the value is not recognized by the enum, it will be stored here, and the raw value is accessible through `.value`.
    """

    @classmethod
    def _missing_(cls, value: typing.Any) -> "IoaAction":
        unknown = cls._UNKNOWN
        unknown._value_ = value
        return unknown

    def visit(
        self,
        monitor: typing.Callable[[], T_Result],
        detect: typing.Callable[[], T_Result],
        block: typing.Callable[[], T_Result],
        _unknown_member: typing.Callable[[str], T_Result],
    ) -> T_Result:
        if self is IoaAction.MONITOR:
            return monitor()
        if self is IoaAction.DETECT:
            return detect()
        if self is IoaAction.BLOCK:
            return block()
        return _unknown_member(self._value_)
